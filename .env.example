# Environment Variables

# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/glucosense

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS (optional)
ALLOWED_ORIGINS=http://localhost:3000,exp://192.168.1.100:8081

# ========================================
# EMAIL CONFIGURATION
# ========================================

# Email Provider (choose one: gmail, sendgrid, smtp)
EMAIL_PROVIDER=gmail

# Option 1: Gmail Configuration
# You need to generate an App Password: https://support.google.com/accounts/answer/185833
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password

# Option 2: SendGrid Configuration (if using sendgrid)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Option 3: Custom SMTP Configuration (if using smtp)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-username
# SMTP_PASSWORD=your-smtp-password

# Email Settings
EMAIL_FROM=noreply@glucosense.com
EMAIL_FROM_NAME=GlucoSense

# Verification Code Settings
VERIFICATION_CODE_EXPIRY=600000
VERIFICATION_CODE_LENGTH=6

# ========================================
# FIREBASE CONFIGURATION
# ========================================

# Option 1: Using Service Account JSON file (recommended for production)
# Download from: Firebase Console > Project Settings > Service Accounts
# FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# Option 2: Using individual environment variables (easier for deployment)
# Get these from your Firebase service account JSON file
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# Note: For FIREBASE_PRIVATE_KEY, copy the entire private key including "-----BEGIN PRIVATE KEY-----" and "-----END PRIVATE KEY-----"
# If using .env file, keep it as one line with \n for line breaks
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----\n"
